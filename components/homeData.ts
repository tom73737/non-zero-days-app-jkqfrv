
// This file is kept for backwards compatibility but is no longer used
// The app now uses the habit tracking interface instead of modal demos

export const modalDemos: any[] = [];

export type ModalDemo = {
  title: string;
  description: string;
  route: string;
  color: string;
};
